spring:
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://localhost:3306/petpeers
   # username: give you db username
   # password: give your db password
    driver-class-name: com.mysql.cj.jdbc.Driver
    
  profiles:
    active:
    - default
#spring.jpa.show-sql: true
